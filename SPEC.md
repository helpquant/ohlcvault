# SPEC.md — OHLCVault 数据契约 v1

> **本文件是数据层与客户端之间唯一权威的契约。**
>
> 两侧的关系是：`pipeline/`（采集与校验）**写入**符合本契约的数据；`ohlcvault`（开源客户端）**读取**符合本契约的数据。两侧必须能各自独立演化，唯一不能变的是本文件。
>
> 修改数据的形状之前，先改这里。改完再改代码。
>
> - 契约的可执行样本 → 见 `schema/example-*.json`

---

## 0. 契约的地位、版本与用词

### 0.1 三个层级，三份工件

| 层 | 工件 | 权威性 | 变更频率 |
|---|---|---|---|
| 契约 | `SPEC.md`（本文件） | 唯一权威 | 极少，且必须先改 |
| 样本 | `schema/example-*.json` | 契约的可执行副本 | 随契约同步 |
| 数据 | `daily/` `meta/` `snapshots/` | 契约的实例 | 每天 |

`schema/` 下的样本**不是示意，是契约本身**——它们由真实产物机械生成（`pipeline/gen_schema_samples.py`），任何字段差异都视为契约违规。手改样本文件即破坏契约。

样本的**数组被截断**到前若干行以控制体积，但字段集合与真实文件**完全一致**。不得据样本推断数组长度。

### 0.2 版本号策略

| 规则 | 说明 |
|---|---|
| 每个独立工件都有自己的整型 `schema` 字段 | 月分片、symbols、calendar、manifest、snapshot 各自独立演进 |
| **只增不改** | 新增可选字段 → `schema` 不变；改语义 / 删字段 / 改类型 → `schema` +1 |
| 客户端**必须**拒绝 `schema` 大于自身支持上限的文件 | 宁可报错，不可猜测 |
| 客户端**必须**接受 `schema` 更小的文件 | 向后兼容是读取方的责任 |
| v1 的 `schema` 全部为 `1` | |

**未知字段必须被忽略，不得报错。** 这是「只增不改」得以成立的前提：写入方新增字段时，旧客户端不能因此崩溃。

### 0.3 用词

本文中 **必须 / MUST**、**不得 / MUST NOT**、**应该 / SHOULD** 按 RFC 2119 语义使用：前者是契约违规（会导致校验失败），后者是建议（违反不报错）。

**整数** 指 JSON number 且无小数部分；本文所有价格、量、因子、日期都是整数。

---

## 1. 市场与命名空间

### 1.1 市场代码

| 代码 | 市场 | 时区 | 货币 | 状态 |
|---|---|---|---|---|
| `cn` | A 股（沪 / 深） | `Asia/Shanghai` | CNY | V1 |
| `hk` | 港股 | `Asia/Hong_Kong` | HKD | V1 |
| `us` | 美股 | `America/New_York` | USD | V1 |

市场代码**全小写**，是路径与文件头的组成部分。**不得**使用 `a` / `china` / `hkex` 等别名。

### 1.2 命名空间（namespace）

同一个市场里有两种性质不同的证券，**必须存放在不同的文件里**：

| 取值 | 含义 | 路径形态 | 说明 |
|---|---|---|---|
| `stock` | 非指数证券（股票、ETF、可转债） | `daily/{market}/` | 有真实成交量与成交额，单位是「股 / 元」 |
| `index` | 指数 | `daily/idx/{market}/` | 成交量口径与个股**不同源**，见 §9.3 |

**为什么必须分开（这不是洁癖，是正确性要求）：**

1. **单位口径冲突**：指数与个股的成交量不是同一个东西（实测 TDX 源下差 10⁴ 倍）。文件头只有一个 `volume_unit` 声明，一个文件就不能装两种口径。
2. **查询污染**：实测按成交额排序的当日 Top 8 **全部是指数**（深证成指 10,924 亿、上证指数 9,942 亿……），个股排行第一屏被完全挤掉。
3. **API 语义**：`ov.daily()` 不该返回指数，`ov.index_daily()` 不该返回个股。分开之后这条规则由路径保证，而不是由客户端过滤保证。

> `namespace` 描述的是**文件的性质**，不是单个标的的性质。一个 `namespace="stock"` 的文件里允许出现 `type` 为 `stock` / `etf` / `bond` 的标的（精确类型查 `meta/symbols/`），但**不得**出现指数。V1 的 `stock` 文件里只有 `stock`。

---

## 2. 证券代码（Symbol）规范

### 2.1 两条规则，各有各的场合

| 场合 | 形态 | 样例 |
|---|---|---|
| **存储层**（数据文件、清单、路径） | 前缀式，**小写**：`{ex}.{code}` | `sh.600519` `sz.000001` `sh.000001` `hk.00700` `us.AAPL` |
| **对外 API**（客户端入参、返回值、文档） | 后缀式，**大写交易所**：`{code}.{EX}` | `600519.SH` `000001.SZ` `000001.SH` `00700.HK` `AAPL.US` |

### 2.2 交易所代码

| 存储前缀 | API 后缀 | 含义 |
|---|---|---|
| `sh` | `SH` | 上海证券交易所（含指数、B 股） |
| `sz` | `SZ` | 深圳证券交易所（含指数、B 股） |
| `bj` | `BJ` | 北京证券交易所（**V1 无数据**，见 §5.4） |
| `hk` | `HK` | 香港交易所 |
| `us` | `US` | 美国市场（不细分 NYSE / NASDAQ，见下） |

### 2.3 为什么存储用前缀式（而不是 Tushare 的后缀式）

存储层与 API 层采用不同形态（而不是全用 Tushare 后缀式），理由是实测出来的：

- **`sh.000001` 是上证指数，`sz.000001` 是平安银行。** 前缀式天然携带交易所信息，两者前缀不同、永不歧义；后缀式若简写成 `000001` 则直接冲突。
- **已落盘数据的形态就是前缀式**，且这是唯一能无损表达「同 6 位代码分属两所」的写法。

代价是：对外要多一次转换。**这个代价由客户端承担，不由用户承担**——客户端解析器**必须同时接受两种写法**（§2.4），用户永远不需要记规则。

### 2.4 客户端的解析义务

`ov.*` 的所有入参 `symbol` 参数**必须**接受以下全部写法，并归一化为内部前缀式：

| 用户输入 | 归一化结果 |
|---|---|
| `600519.SH` / `600519.sh` | `sh.600519` |
| `sh.600519` | `sh.600519` |
| `sh600519` | `sh.600519` |

**歧义规则（必须实现，不得静默猜测）：** 输入无任何交易所信息时（如 `000001`），**必须**报错并要求用户补全，**不得**默认猜测为深市或沪市。唯一的例外是 `hk` / `us` 市场的调用上下文已由 `market=` 参数确定时。

**港股代码必须零填充到 5 位**：`00700`、`00005`。这是港交所的规范写法，也是 akshare / yfinance 的共同要求。

**美股不加交易所后缀**：存储为 `us.AAPL`，API 为 `AAPL.US`。不区分 NYSE / NASDAQ——该信息不进主键，而记录在 `meta/symbols/us.json` 的 `exchange` 字段里（`XNYS` / `XNAS`）。

---

## 3. 对象路径规则

```
latest.json                                  # 可变指针 → 当前快照
meta/manifest.json                           # 目录：有哪些市场、多少标的、哪些月
meta/symbols/{market}.json                   # 标的清单（含指数、含退市股）
meta/calendar/{market}.json                  # 交易日历
daily/{market}/{YYYY-MM}.json.gz             # 月分片 · stock 命名空间
daily/idx/{market}/{YYYY-MM}.json.gz         # 月分片 · index 命名空间
snapshots/{sid}/root.json                    # 快照摘要
snapshots/{sid}/manifest.json                # 快照事实清单（逐文件 sha256）
changes/{sid}.json                           # 相对父快照的增量
```

规则：

- **一律全小写**。R2 / S3 / CDN 的 URL 区分大小写，统一小写消除跨平台事故。
- **一律 `.json.gz`**，不落明文。见 §3.1。
- **`{market}` 是 §1.1 的市场代码**，不是交易所代码。
- **指数额外多一层 `idx/`**，而不是把 `idx` 当作 market 值。因为指数的市场属性仍然存在（A 股指数、港股指数、美股指数各有各的交易日历），把 market 挤掉会导致无法关联日历。

### 3.1 为什么必须显式 `.json.gz`，而不是靠 CDN 动态压缩

实测（`tools/bench_cross_section.py`）：

| 项 | 实测值 |
|---|---|
| 明文 JSON | 4.58 MB |
| gzip(9) | **1.42 MB**（3.21×） |
| `json.loads` | 28.7 ms |
| `gzip.decompress` + `loads` | 31.1 ms（**只多 2.4 ms**） |

三条结论：

1. **落盘即 gzip**：省 3.2× 存储与带宽，代价 2.4 ms 装载时间。
2. **不能依赖 CDN 动态压缩**：`urllib` / `requests` 默认不发 `Accept-Encoding`，拿不到 Cloudflare 的自动压缩——不显式存 `.gz`，实际传输的就是 4.58 MB 而不是 1.42 MB。
3. **sha256 的对象必须是「客户端实际下载到的字节」**。若压缩发生在中间层，校验对象就含糊了。显式 `.gz` + 对 `.gz` 字节做 hash，是唯一干净的契约。

### 3.2 gzip 头 `MTIME` 必须固定为 0（强制）

`gzip` 格式的文件头里有一个 `MTIME` 字段。**各语言实现的默认行为都是写入「当前时间」**（Python 的 `gzip.compress()` 即如此），后果是：

> 同样的输入，每次压缩产出**不同的字节** → sha256 每次都变 → 快照 `sid` 每轮都不同 → §8.3 承诺的「任何人在任何机器上重建必然得到同一个 sid」直接失效。

**写入方必须**显式固定 `MTIME = 0`（Python：`gzip.compress(raw, level, mtime=0)`），使压缩输出成为输入的纯函数。

这条实测踩过：M1 期间快照 sid 在两次完全相同的构建中给出了不同值，排查后定位到这一个字段。**它是整套「可复现」立论里唯一致命且最容易被忽略的字节。**

同理，`MTIME` 之外的 gzip 头字段（OS 标识、XFL）由库固定，无需干预；但**不得**使用会在头里写环境信息的压缩工具。

### 3.3 分发的 HTTP 语义（强制）

| 项 | 值 | 理由 |
|---|---|---|
| `Content-Type` | `application/gzip` | 传的就是 gzip 字节，如实声明 |
| `Content-Encoding` | **不得设置** | 见下 |
| `Cache-Control` | 按 §4.4 的当月 / 已封月二分 | |

**为什么绝不能设 `Content-Encoding: gzip`**：一旦声明，符合规范的 HTTP 客户端（`requests`、浏览器）会**自动解压并在响应里去掉该头**。于是客户端拿到的是明文 JSON，而它在清单里比对的 `sha256` 是对 `.gz` 字节算的——**校验必然失败**，而失败原因看起来像「数据被篡改」。更糟的是，这个失败只在特定客户端上复现，极难定位。

按 `application/gzip` 原样传字节，让「传输的字节 = 校验的字节」这件事在协议层就成立。压缩由数据集自己负责（§3.1），**不由传输层负责**——这两件事不能混。

**客户端必须**按 `.json.gz` 解压后解析；**不得**假设文件是明文 JSON。

---

## 4. 月分片 schema（`daily/`）

一个市场一个月一个文件，装着该市场**全部标的**在该月的**全部交易日**。

### 4.1 文件头

```jsonc
{
  "schema": 1,
  "market": "cn",
  "namespace": "stock",
  "period": "2026-09",
  "adjust": "none",
  "price_scale": 1000,
  "currency": "CNY",
  "volume_unit": "share",
  "af_scale": 1000000,
  "af_basis": "back_cumulative_from_listing",
  "day_boundary": "exchange_settlement",
  "source": "baostock",
  "updated": "2026-09-21T16:30:00+08:00",
  "counts": { "symbols": 5219, "rows": 73066 },
  "symbols": [ /* block… */ ]
}
```

| 字段 | 类型 | 必须 | 说明 |
|---|---|---|---|
| `schema` | int | ✔ | 本文件版本，当前 `1` |
| `market` | str | ✔ | §1.1 市场代码 |
| `namespace` | str | ✔ | §1.2，`stock` 或 `index` |
| `period` | str | ✔ | `YYYY-MM`，**必须**与文件名一致 |
| `adjust` | str | ✔ | **恒为 `"none"`**。本数据集只存不复权原始价，见 §9.2 |
| `price_scale` | int | ✔ | 价格定点标度，`1000` |
| `currency` | str | ✔ | ISO 4217，`CNY`/`HKD`/`USD`。价格与成交额共用 |
| `volume_unit` | str | ✔ | 恒为 `"share"`（股）。见 §9.3 |
| `af_scale` | int | ◐ | 因子定点标度，`1000000`。`af_basis="unavailable"` 时**不得**出现 |
| `af_basis` | str | ✔ | `back_cumulative_from_listing` 或 `unavailable` |
| `day_boundary` | str | ✔ | 恒为 `"exchange_settlement"`（一根 K 线 = 一个交易所交易日） |
| `source` | str | ✔ | 采集源标识，多源用 `+` 连接，如 `baostock+pytdx` |
| `updated` | str | ✔ | ISO 8601 带时区偏移。**必须从数据派生**，见 §4.5 |
| `counts` | obj | ○ | 便捷统计，可省略；**不得**用于校验（权威是数组长度） |
| `symbols` | array | ✔ | block 数组，**按 `s` 升序** |

> ◐ = 条件必须。○ = 可选。

### 4.5 `updated` 必须是确定性的（强制）

`updated` **不得**写「脚本运行时刻」。它**必须**从数据自身派生——推荐用法是本文件最后交易日的收盘时刻，例如 `"2026-09-21T15:00:00+08:00"`。

两个理由，第二个是硬的：

1. **语义更准**。「这份内容截至哪个交易日」本来就比「脚本何时跑的」更贴近这个字段该有的含义。
2. **可复现性的必要条件**。写入方每次运行都产生新时间戳 → 文件字节每次都变 → sha256 每次都变 → 快照 `sid` 永不稳定 → §8.3 的承诺失效。这与 §3.2 的 gzip `MTIME` 是同一类错误：**任何进入文件字节的墙钟时间，都是可复现性的敌人。**

> 实测踩过两次：`meta/manifest.json` 的 `updated` 与 gzip 头 `MTIME`。两者都曾让「重复构建得到同一 sid」失败。凡是写进文件的时间，都要先问一句「它会让同一份内容产生不同字节吗」。

### 4.2 block（`symbols` 的元素）

列式存储：字段名只出现一次，之后全是同质数组。行式（Tushare 风格，每行重复 key）实测体积膨胀 3–5 倍，**禁止**。

```jsonc
{
  "s": "sh.600519",
  "d": [20260901, 20260902],
  "o": [1523000, 1528000],
  "h": [1532000, 1531000],
  "l": [1519000, 1522000],
  "c": [1528500, 1524000],
  "v": [3120000, 2870000],
  "a": [4739200000, 4346800000],
  "af": [1000000, 1000000]
}
```

| 键 | 类型 | 长度 | 说明 |
|---|---|---|---|
| `s` | str | — | §2.1 存储式代码 |
| `d` | int[] | N | 交易日 `YYYYMMDD`，**升序、唯一**，且**必须**落在本文件 `period` 之内 |
| `o` | int[] | N | 开盘价 × `price_scale` |
| `h` | int[] | N | 最高价 × `price_scale` |
| `l` | int[] | N | 最低价 × `price_scale` |
| `c` | int[] | N | 收盘价 × `price_scale` |
| `v` | int[] | N | 成交量（**股**），可为 `null` |
| `a` | int[] | N | 成交额（**元**，本币），可为 `null` |
| `af` | int[] | N | 后复权累计因子 × `af_scale`，可为 `null`。`af_basis="unavailable"` 时**该键必须缺席** |

**硬性约束（客户端校验时必须逐条检查）：**

1. `d` / `o` / `h` / `l` / `c` / `v` / `a` / `af` 存在时**长度必须全部相等**。长度不等 = 文件损坏。
2. `d` **必须**严格递增（无重复、无乱序）。
3. `d[0]` 与 `d[-1]` **必须**在 `period` 覆盖的月份内。
4. `l[i] ≤ min(o[i], c[i])` 且 `h[i] ≥ max(o[i], c[i])`（价格自洽性，`SHOULD`）。
5. 同一文件中 `s` **不得**重复。

### 4.3 因子键：`af` 是唯一合法拼写

> **v1 修正**：早期实现中，pytdx 管线写 `f`、baostock 管线写 `af`，同一个字段两种拼写。本契约定案为 **`af`**，`f` **不得**再出现。

命名理由：`af` 与头部字段 `af_scale` / `af_basis` 同前缀，读文件头就能推断列表里该找什么；`f` 过于笼统（可能被误读为 frequency / flag / factor of what）。

### 4.4 月分片的写入与缓存语义

| 状态 | 判定 | 写入 | 缓存 |
|---|---|---|---|
| **当月**（open） | `period == 当前月` | 每个交易日**整文件重写** | `max-age=300, stale-while-revalidate=86400` |
| **已封月**（sealed） | `period < 当前月` | **永不重写** | `max-age=31536000, immutable` |

**已封月不得重写**是整套设计的地基：正因为历史文件永不变，`immutable` 缓存才安全，用户才有可能「一生下载一次」。

**唯一的例外是交易所追溯修正成交数据**（极罕见）。此时文件内容变了、sha256 变了，客户端按 §10.3 的规则重下即可——**不需要为此改变缓存策略**。

---

## 5. `meta/symbols/{market}.json` — 标的清单

一个市场一个文件，**包含该市场的全部标的：股票（含退市）+ 指数 + ETF + 可转债**。

### 5.1 文件结构

```jsonc
{
  "schema": 1,
  "market": "cn",
  "updated": "2026-09-21T16:30:00+08:00",
  "source": "baostock:query_stock_basic",
  "symbol_format": "exchange_prefix",
  "coverage": {
    "note": "不含北京证券交易所标的（上游源未提供）",
    "complete": false
  },
  "counts": {
    "total": 8969,
    "by_type": { "stock": 5558, "index": 597, "etf": 1678, "bond": 1136 },
    "stock": { "listed": 5221, "delisted": 337 }
  },
  "supplements": [
    { "s": "sh.000688", "name": "科创50", "ipo": 20191231,
      "reason": "baostock query_stock_basic 未收录；由 akshare 新浪源补口" }
  ],
  "symbols": [ /* entry… */ ]
}
```

`supplements`（可选）记录**上游源缺口的人工补齐项**。它的存在是因为：清单侧一旦漏了数据侧真实存在的标的，用户就会遇到「数据里有、清单里查不到」的错位。每一项 `必须` 带 `reason`，否则它会退化成无人维护的口头知识。客户端 `必须` 忽略此字段。

### 5.2 entry

```jsonc
{
  "s": "sh.600519",
  "api": "600519.SH",
  "code": "600519",
  "exchange": "SH",
  "name": "贵州茅台",
  "type": "stock",
  "board": "main",
  "ipo": 20010827,
  "out": null,
  "status": "listed"
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `s` | str | 存储式代码（§2.1），文件内**唯一** |
| `api` | str | API 式代码（§2.1），由 `s` 机械推导，供客户端免转换直接展示 |
| `code` | str | 原始代码，**保留前导零**（港股 `00700`） |
| `exchange` | str | 大写交易所代码（§2.2） |
| `name` | str | 名称。中文市场为中文名，美股为英文名。UTF-8 |
| `type` | str | `stock` \| `index` \| `etf` \| `bond` \| `other` |
| `board` | str\|null | 仅 `type="stock"` 且有意义时给出：`main` \| `gem`（创业板）\| `star`（科创板）\| `neeq`（北交所）。其余为 `null` |
| `ipo` | int\|null | 上市日 `YYYYMMDD`。指数取基日 |
| `out` | int\|null | **退市日** `YYYYMMDD`；未退市为 `null` |
| `status` | str | `listed` \| `delisted` |

**`status` / `out` 的语义**：`status="delisted"` 时 `out` **必须**是整数；`status="listed"` 时 `out` **必须**是 `null`。指数与 ETF 通常恒为 `listed`。

### 5.3 退市股必须入池（幸存者偏差）

**退市股不得被过滤掉。** 这是本数据集相对「只爬当前在售列表」的爬虫类数据源的**结构性优势**，也是「可信历史存档」这个立场的必要成分：

- 回测池若只含今天还活着的股票，历史收益率会被**系统性高估**（生存者偏差）。
- 已退市标的的日线在其 `ipo ~ out` 区间内是**完整且真实**的，没有任何理由丢弃。

因此：`daily/` 的采集池 = `type in (stock, etf)` 的**全部**标的（`status` 不限），只是退市股在其退市日之后自然没有数据。

### 5.4 已知覆盖缺口（必须显式声明，不得隐瞒）

| 缺口 | 现状 | 处理 |
|---|---|---|
| 北京证券交易所 | 上游 `baostock` 的 `query_stock_basic` **不返回** `bj.*` 标的 | `coverage.complete = false` + `coverage.note` 写明；后续补源后翻转 |
| 港股 / 美股 | V1 采集源未定 | 文件可以缺席 |

**`coverage.complete = false` 时客户端 `SHOULD` 在首次使用时提示用户。** 一个数据项目隐瞒覆盖缺口，比覆盖不全更严重。

---

## 6. `meta/calendar/{market}.json` — 交易日历

```jsonc
{
  "schema": 1,
  "market": "cn",
  "timezone": "Asia/Shanghai",
  "source": "baostock:query_trade_dates",
  "range": [20000104, 20261231],
  "count": 6543,
  "days": [20000104, 20000105 /* … */ ]
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `market` | str | 市场代码 |
| `timezone` | str | IANA 时区名 |
| `source` | str | `baostock:query_trade_dates`（A 股）/ `derived:index_bars`（港美，见下） |
| `range` | [int, int] | `[首个交易日, 末个交易日]` |
| `count` | int | `days` 长度（冗余，便于免解析检查） |
| `days` | int[] | 交易日 `YYYYMMDD`，**升序、唯一** |

### 6.1 日历的两种来源

| 市场 | 来源 | 说明 |
|---|---|---|
| `cn` | `baostock:query_trade_dates` | 权威，**且含未来排班**（实测覆盖到 2026-12-31） |
| `hk` / `us` | `derived:index_bars` | **从基准指数日线派生** |

**为什么港美要派生而不是查表**：从指数日线派生是**自洽**的（日历永远不会与数据矛盾）、**零额外依赖**，而且**自动处理港股台风停市**这类无规律事件——任何静态节假日表都做不到。

### 6.2 日历不承诺「未来」

`days` 里可以包含未来日期（A 股来自交易所排班表），但**客户端不得依赖它**：任何未来日期都可能因临时休市而变。日历的**权威区间只到 `max(days 中 ≤ 今天的日期)`**。

---

## 7. `meta/manifest.json` — 目录

**控制面**，体积目标 < 5 KB。它回答「有哪些市场、多少标的、有哪些月、最新到哪天」，**但不含逐文件哈希**——那是快照的职责（§8）。

> 这不是冗余。**全量 manifest 每天存一份是错的**：10 年累积约 200 MB 纯元数据，而这份清单其实可由快照完全推导。因此把「目录」与「指纹」拆开：目录很小、随标的池变化而变；指纹很大、每天变。**两套哈希 = 两套真相，禁止**——指纹只有一个来源，即快照。

```jsonc
{
  "schema": 1,
  "updated": "2026-09-21T16:30:00+08:00",
  "price_scale_default": 1000,
  "af_scale_default": 1000000,
  "markets": {
    "cn": {
      "name": "A股",
      "timezone": "Asia/Shanghai",
      "currency": "CNY",
      "symbols": { "path": "meta/symbols/cn.json", "count": 8969 },
      "calendar": { "path": "meta/calendar/cn.json", "count": 6543 },
      "monthly": {
        "stock": [ { "period": "2026-07", "path": "daily/cn/2026-07.json.gz", "status": "sealed" } ],
        "index": [ { "period": "2026-07", "path": "daily/idx/cn/2026-07.json.gz", "status": "sealed" } ]
      },
      "last_trade_date": 20260918
    }
  }
}
```

`monthly[namespace]` 按 `period` 升序。`status` ∈ `open`（当月）\| `sealed`（已封月）。

### 7.1 manifest **不得**包含 `snapshot` 字段

这看起来像遗漏，其实是必须的。`meta/manifest.json` 本身是被快照哈希覆盖的数据文件，如果它再引用某个快照 id，就构成循环依赖：算出 `sid` 需要知道 manifest 的内容，而 manifest 的内容又需要先知道 `sid`。

**指针的唯一来源是 `latest.json`**（§8.1）。客户端流程 `latest.json → sid → snapshots/{sid}/manifest.json` 从不需要从目录反查快照，所以去掉这个字段没有损失，只消除了一个真实的构建顺序问题。

---

## 8. 快照与内容寻址

### 8.1 `latest.json` — 可变指针

```jsonc
{
  "schema": 1,
  "snapshot": "47d298f672978ac9",
  "created": "2026-09-20T23:46:50+00:00",
  "root": "snapshots/47d298f672978ac9/root.json",
  "manifest": "snapshots/47d298f672978ac9/manifest.json",
  "file_count": 3,
  "total_bytes": 18561498
}
```

**唯一允许变化的工件**，缓存 `max-age=60`。

### 8.2 `snapshots/{sid}/manifest.json` — 事实清单

**sha256 的唯一权威来源。**

```jsonc
{
  "schema": 1,
  "snapshot": "47d298f672978ac9",
  "parent": null,
  "created": "2026-09-20T23:46:50+00:00",
  "content_root": "47d298f672978ac9897b016e3016ce00d846588294d748634675bdb149f68e97",
  "file_count": 3,
  "total_bytes": 18561498,
  "files": {
    "daily/cn/2026-07.json.gz": { "sha256": "fff342…", "size": 7287414 }
  }
}
```

**`sha256` 必须对客户端实际下载到的字节计算**——即 `.gz` 文件的原始字节，**不得**先解压再算，**不得**对明文 JSON 算。

### 8.3 `sid` 的构造（可复现性的根）

```
sid = sha256( 对每个文件取 "路径:sha256:size" 行 → 排序 → 拼接 )[0:16]
```

任何人在任何机器上对同一份数据重建，**必然**得到同一个 `sid`。这是「可复现」而不是「号称可复现」的分界。

### 8.4 快照不是数据副本

快照是**元数据包**。数据文件本身躺在 `daily/`。快照只回答「这一刻的数据集长什么样、每份文件的确切指纹是什么」。

---

## 9. 单位、标度与复权

### 9.1 定点整数的理由

**浮点数不精确，且不同实现的舍入策略不同——一份声称「逐字节可复现」的数据集里不能有浮点。** 所有价格、量、因子一律存为整数，乘以固定标度。

| 量 | 标度 | 还原 |
|---|---|---|
| 价格 `o/h/l/c` | `price_scale = 1000` | `price = o[i] / 1000` |
| 因子 `af` | `af_scale = 1000000` | `af = af[i] / 1000000` |
| 量 `v` | 1（整数股） | 直接用 |
| 额 `a` | 1（整数元） | 直接用 |

`price_scale = 1000` 支持到 0.001 元的报价精度，覆盖 A 股（0.01）、港股（0.001）、美股（0.01）三市场的全部报价规则。

### 9.2 只存事实，不存视图（本数据集的第一原则）

**`adjust` 恒为 `"none"`。永远不存前复权 / 后复权价格。**

复权是**视图**，不是**事实**。静态托管只该存事实。原因：前复权的锚点是「最新价」，每次分红送转都会导致**全历史价格被重算** → 所有历史文件天天变 → `immutable` 缓存全部失效 → 整套设计的收益归零。

**复权是客户端的纯函数：**

```
后复权 hfq_i  = close_i × af_i / af_scale
前复权 qfq_i  = close_i × af_i / af_latest
不复权        = close_i
```

`af_latest` **必须**取「该标的最后一个有数据的交易日的因子」，**不是**「当前日期的因子」——因为停牌期间不产生新因子。

### 9.3 `volume_unit`：量已归一为股

`volume_unit` 对**所有**文件恒为 `"share"`（股）。上游的原始单位由采集方归一，契约只保证落盘后的口径：

- A 股的通用换算：**1 手 = 100 股**。
- 指数与个股的成交量**不是同一个量**（指数是成分股成交量的汇总，含义也不同），归一化乘数随证券类型而变。因此**采集方不得**在同一个文件里混用两种乘数——这正是 §1.2 要求指数独立成文件的直接原因之一。

> 各上游源返回的原始单位与具体换算乘数属于采集端实现细节，不进本契约。核对数据时依据文件头的 `volume_unit` 与本节规则即可。

### 9.4 停牌与缺失值

| 情形 | 表示 |
|---|---|
| 停牌日 | **该日不产生行**（源无数据即不入库），而不是填 0 |
| 源的某列缺失 | 对应数组元素为 `null` |
| 标的当月无数据 | 该标的**不出现在** `symbols` 数组里（不要给空 block） |

**不得**用 `0` 表示缺失：`0` 是合法价格（虽然罕见），`0` 与「没有」必须可区分。

### 9.5 港美 V1 无因子

港美市场**没有免费官方复权因子源**。因此：

- `af_basis` = `"unavailable"`
- block 中 **`af` 键必须缺席**（不是全 `null`）
- 客户端遇到 `af_basis == "unavailable"` 时，`ov.adjust(df, to="qfq")` **必须**抛出明确错误，**不得**静默返回未复权数据——**宁可报错，不给不可信的数字。**

### 9.6 日期与时刻

- **交易日期**恒为整数 `YYYYMMDD`。这是天然可排序、可直接比较的紧凑表示。
- `updated` / `created` 是 ISO 8601 **带时区偏移**的字符串，用于审计，不用于业务逻辑。

---

## 10. 客户端读取算法

### 10.1 标准读取流程

```
1. GET latest.json                     → 拿到 sid
2. GET snapshots/{sid}/manifest.json   → 拿到 file → sha256 映射
3. 对每个需要的文件：
   a. 查本地缓存，sha256 与 manifest 一致  → 直接使用（零网络）
   b. 否则依序尝试镜像，下载到临时文件
   c. 计算 sha256，与 manifest 比对
      - 一致 → 落缓存
      - 不一致 → 视为脏数据，丢弃，试下一个镜像
4. 拼接（时间序列跨月 / 断面转置）
```

### 10.2 镜像顺序

主镜像由使用者指定（默认 R2 自定义域）。其余为降级候选。**成功过的镜像会被提到首选位**，后续请求不再重复探测死镜像——否则每次请求都要先付一次失败超时。

### 10.3 缓存有效性由哈希决定，与时间无关

**不需要 TTL 启发式，不需要猜「这份缓存过期没有」。** 哈希一致即有效，不一致即无效。这是内容寻址的直接好处。

| 文件 | 下载次数 | 判定 |
|---|---|---|
| `daily/{已封月}` | **一生一次** | 命中且哈希一致 → 零网络 |
| `daily/{当月}` | 每交易日一次 | 哈希每天变 → 自动重下 |
| `latest.json` | 每次启动 | 控制面极小 |

**重下前旧缓存不得删除**，改名 `.bak` 保留一次——防止下载中途失败把好缓存弄丢。这是实测过的失败模式。

### 10.4 客户端必须做的校验（按代价排序）

| 校验 | 代价 | 拦住的错误 |
|---|---|---|
| sha256 比对 | 一次哈希 | 传输截断、CDN 脏数据、镜像投毒 |
| `schema` 上限 | 一次比较 | 读到未来版本 |
| 数组长度一致 | O(1) | 文件损坏 |
| `d` 严格递增 | O(N) | 拼接后乱序 |
| `d ∈ period` | O(1) | 月分片串月 |

前两条是**必须无条件执行**的；后三条在 `validate=True` 时执行（默认开）。

---

## 11. 版本兼容策略

| 变更类型 | `schema` | 旧客户端行为 |
|---|---|---|
| 新增可选字段 | 不变 | 忽略，正常工作 |
| 新增 `type` 枚举值 | 不变 | 应降级为 `other`，不得崩溃 |
| 新增市场 / 命名空间 | 不变 | 应忽略未请求的市场 |
| 字段语义变更 | **+1** | **拒绝** |
| 删除字段 / 改类型 | **+1** | **拒绝** |
| 整数标度变更 | **+1** | **拒绝** |

**`price_scale` 与 `af_scale` 是文件头字段而非常量**，正是为了将来换标度时不必改 `schema`——客户端永远读文件头，不硬编码。

---

## 12. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-21 | v1 定稿。确立：指数独立命名空间 `daily/idx/`；`af` 为唯一合法因子键（废止 `f`）；存储前缀式 / API 后缀式的双轨规范；文件头字段统一（`af_scale`、`af_basis`、`currency`、`updated`）；退市股强制入池；显式 `.json.gz`；`meta/manifest.json` 与快照指纹分离。 |

### 12.1 v1 定稿时推翻的三处早期约定

本契约定稿时推翻了早期草案的三处写法，均**以本文件为准**：

| 早期约定 | 定稿 | 理由 |
|---|---|---|
| 统一 Tushare 后缀式（`600519.SH`） | 存储用**前缀式**（`sh.600519`），API 用后缀式 | 见 §2.3 |
| `meta/symbols/idx.json` 单列指数 | 指数并入 `meta/symbols/{market}.json`，用 `type` 区分 | 指数有市场归属，单列会导致跨文件引用 |
| 因子键写作 `f` | 改为 `af` | 见 §4.3 |
